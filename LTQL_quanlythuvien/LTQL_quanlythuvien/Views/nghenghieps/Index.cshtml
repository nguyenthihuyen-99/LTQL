@model IEnumerable<LTQL_quanlythuvien.Models.nghenghiep>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.congty)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hoten)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.congty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hoten)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CCCD }) |
            @Html.ActionLink("Details", "Details", new { id=item.CCCD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CCCD })
        </td>
    </tr>
}

</table>
